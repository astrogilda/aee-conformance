// Module path is a NEUTRAL PLACEHOLDER (finalized at landing; see BUILD-NOTES.md).
// The core packages in this module are stdlib-only by design: an import-list test
// (aee/imports_test.go) enforces zero go-witness imports in the verification core.
module github.com/astrogilda/aee-conformance

go 1.24
